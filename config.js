const config = {
    webhook: "SUA WEBHOOK AQUI",
    time: 1, // in min

    connection: {
        host: "127.0.0.1",
        user: "root",
        password: "",
        database: "vrp",
    },

    embed: {
        color: "BLACK"
    }
}

module.exports = config